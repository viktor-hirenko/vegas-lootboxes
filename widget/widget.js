// Entry point — bootstrapped fully in a later step once protocol/rendering
// modules exist (see modules/). Placeholder kept minimal so the page never 500s.
console.info('[vegas-lootboxes-widget] scaffold placeholder — wiring modules next.');
