{
  "name": "vegas-lootboxes-widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Draft iFrame widget for Vegas Lootboxes cards. Vanilla HTML/CSS/JS, no runtime frameworks. Tooling here is optional dev convenience only.",
  "scripts": {
    "dev": "node scripts/serve.js",
    "build": "node scripts/build.js",
    "serve:dist": "node scripts/serve.js dist"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "sharp": "^0.35.3"
  }
}
